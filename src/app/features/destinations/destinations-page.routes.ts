import { Routes } from '@angular/router';
import { DestinationsPage } from './destinations-page/destinations-page';

export const DESTINATIONS_ROUTES: Routes = [
  { path: '', component: DestinationsPage }
];
